"""Fault debugging — validate the live state of a resource (config + CloudWatch alarms + recent
Logs errors + recent CloudTrail changes), correlate into root-cause hypotheses, and propose fixes.
Read-only diagnosis; the fix output is shaped by the action posture (advisory/artifacts/guarded)."""
