"""Service review & optimization — per-service best-practice checks over live config + sizing +
CloudWatch metrics (and, for Lambda, code), grounded in deterministic RULES with AWS-doc links.
The LLM narrates/prioritizes; the findings come from the rules (exact)."""
